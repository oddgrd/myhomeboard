[tools]
node = "14"
yarn = "1.22"

[tasks.server-dev]
dir = "server"
run = "docker compose up -d && yarn dev:setup && yarn dev"

[tasks.client-dev]
dir = "client"
run = "yarn dev"

[tasks.server-test]
run = "docker compose up -d && yarn --cwd server test && docker compose down"
