query GetUser($id: String!) {
  getUser(id: $id) {
    name
    avatar
    createdAt
    problems {
      boardId
      consensusGrade
      consensusRating
      ascents {
        grade
        rating
      }
    }
    ascents {
      attempts
      rating
      grade
    }
  }
}
