mutation CreateLayout(
  $file: Upload!
  $title: String!
  $description: String!
  $boardId: String!
) {
  createLayout(
    file: $file
    title: $title
    description: $description
    boardId: $boardId
  ) {
    boardId
    title
    description
    url
    creatorId
  }
}
