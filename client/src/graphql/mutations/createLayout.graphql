mutation CreateLayout(
  $file: Upload!
  $title: String!
  $description: String!
  $boardSlug: String!
) {
  createLayout(
    file: $file
    title: $title
    description: $description
    boardSlug: $boardSlug
  ) {
    boardSlug
    title
    description
    url
    creatorId
  }
}
